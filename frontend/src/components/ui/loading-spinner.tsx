/**
 * LoadingSpinner — Simple loading indicator.
 * Replace with animated SVG/CSS when design arrives.
 */

export function LoadingSpinner() {
  return (
    <div role="status" aria-label="Loading">
      {/* TODO: Animated spinner (CSS or SVG) */}
      <span>Loading...</span>
    </div>
  );
}
